---
import BaseLayout from './BaseLayout.astro'
import ReadingProgress from '../components/ReadingProgress.astro'
import TOC from '../components/TOC.astro'
import CanvasArticle from '../components/pretext/CanvasArticle.tsx'
import { t, type Lang } from '../i18n/utils'

interface Props {
  title: string
  date: Date
  tags: string[]
  lang: Lang
  headings: { depth: number; slug: string; text: string }[]
  readingTime: number
  excerpt?: string
}

const { title, date, tags, lang, headings, readingTime, excerpt = '' } = Astro.props
const canvasText = lang === 'zh'
  ? `${excerpt} 这是一段用 Canvas 渲染的文字，由 Pretext 的 layoutNextLine API 驱动，实现了文字环绕不规则形状的效果。每一行的可用宽度根据是否与右侧圆形重叠而动态调整。`
  : `${excerpt} This text is rendered on Canvas, powered by Pretext's layoutNextLine API, demonstrating text wrapping around irregular shapes. Each line's available width adjusts dynamically based on overlap with the circle.`
const dateStr = date.toLocaleDateString(lang === 'zh' ? 'zh-CN' : 'en-US', {
  year: 'numeric', month: 'long', day: 'numeric'
})
---
<BaseLayout title={`${title} — Alicia.dev`} lang={lang}>
  <ReadingProgress />
  <div class="article-layout container">
    <article class="article-content">
      <header class="article-header">
        <h1 class="article-title">{title}</h1>
        <div class="article-meta">
          <time>{dateStr}</time>
          <span>·</span>
          <span>{t(lang, 'article.readTime', { min: readingTime })}</span>
        </div>
        <div class="article-tags">
          {tags.map(tag => <span class="tag">{tag}</span>)}
        </div>
      </header>
      {excerpt && (
        <div class="canvas-quote">
          <CanvasArticle
            client:visible
            text={canvasText}
            font="Maple Mono"
            fontSize={14}
            typewriter={true}
            imageSrc="/img/claude-pixel.svg"
            imageWidth={100}
            imageHeight={84}
          />
        </div>
      )}
      <div class="prose">
        <slot />
      </div>
    </article>
    <TOC headings={headings} lang={lang} />
  </div>
</BaseLayout>

<style>
  .article-layout {
    display: grid;
    grid-template-columns: minmax(0, 680px) 180px;
    gap: 48px;
    justify-content: center;
    padding-top: 32px;
    padding-bottom: 48px;
    max-width: 960px;
    margin: 0 auto;
  }
  @media (max-width: 900px) {
    .article-layout {
      grid-template-columns: 1fr;
    }
  }
  .article-header {
    margin-bottom: 32px;
  }
  .article-title {
    font-size: 28px;
    font-weight: 600;
    line-height: 1.3;
    color: var(--text);
  }
  [data-theme="dark"] .article-title {
    color: var(--accent);
  }
  .article-meta {
    font-size: 13px;
    color: var(--text-muted);
    margin-top: 12px;
    display: flex;
    gap: 8px;
  }
  .article-tags {
    display: flex;
    gap: 8px;
    margin-top: 12px;
  }
  .tag {
    font-size: 11px;
    padding: 3px 10px;
    background: var(--tag-bg);
    color: var(--text-secondary);
    border-radius: var(--radius-md);
  }
  [data-theme="dark"] .tag {
    color: var(--accent);
    border: 1px solid var(--tag-bg);
  }
  .canvas-quote {
    margin-bottom: 24px;
    display: flex;
    justify-content: center;
  }
  .prose {
    font-size: 15px;
    line-height: 1.8;
    color: var(--text-secondary);
  }
  .prose h2 {
    font-size: 22px;
    font-weight: 600;
    color: var(--accent);
    margin-top: 40px;
    margin-bottom: 16px;
  }
  .prose h3 {
    font-size: 18px;
    font-weight: 500;
    color: var(--accent);
    margin-top: 32px;
    margin-bottom: 12px;
  }
  .prose p {
    margin-bottom: 16px;
  }
  .prose code {
    font-size: 13px;
    padding: 2px 6px;
    background: var(--tag-bg);
    border-radius: var(--radius-sm);
  }
  .prose pre {
    padding: 16px;
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    overflow-x: auto;
    margin-bottom: 16px;
  }
  .prose pre code {
    padding: 0;
    background: none;
  }
  .prose ul, .prose ol {
    padding-left: 24px;
    margin-bottom: 16px;
  }
  .prose li {
    margin-bottom: 4px;
  }
</style>
